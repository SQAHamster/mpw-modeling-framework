context Territory::initTerritory(columns: EInt, rows: EInt)

/** size must be > 0 */
precondition: 
    columns > 0 and rows > 0;

/** size of territory is set */
postcondition: 
    self.stageSize.columnCount = columns and self.stageSize.rowCount = rows;