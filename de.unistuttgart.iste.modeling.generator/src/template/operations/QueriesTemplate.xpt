«IMPORT ecore»

«EXTENSION template::util::GeneratorExtensions»

«DEFINE QueryDefinitions FOR EClass»
	  «FOREACH eAllOperations.select(o|o.isQuery()) AS q»
	  @Override
	  public «q.operationToJavaType()» «q.name»() {
	      throw new RuntimeException("not implemented");
	  }
      «ENDFOREACH»
«ENDDEFINE»