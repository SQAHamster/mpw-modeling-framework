«IMPORT ecore»

«EXTENSION template::util::GeneratorExtensions»

«DEFINE CommandDefinitions FOR EClass»
	  «FOREACH eAllOperations.select(o|o.isCommand()) AS c»
	  @Override
	  public void «c.name»() {
	      new «c.name.toFirstUpper()»Command(this).execute();
	  }
      «ENDFOREACH»
«ENDDEFINE»