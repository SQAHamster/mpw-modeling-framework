import ecore;

create List[String] GeneratedFiles() : (List[String]) {};

addGeneratedSourceFile(String filePath) : GeneratedFiles().add(filePath) -> "";

getGeneratedSourceFiles() : GeneratedFiles();
