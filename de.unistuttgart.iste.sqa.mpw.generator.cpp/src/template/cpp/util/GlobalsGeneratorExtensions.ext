import ecore;
import commandBehavior;
import queryBehavior;

/*
 * Extensions to obtain global variables.
 * Note: currently duplicated, later refactored into central place.
 */

String getModulePackage() : GLOBALVAR module_package;
